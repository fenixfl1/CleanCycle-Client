// axios-error.d.ts

import { AxiosError } from "axios";

declare module "axios-error" {
  interface AxiosError {
    // You can add additional properties or methods to the AxiosError type here if needed.
  }
}
